import { scale } from "../utils/scale";

export const normalisedFontSize = {
  tiny: scale(8),
  small: scale(12),
  normal: scale(14),
  large: scale(18),
  extraLarge: scale(24),
  huge: scale(32),
}